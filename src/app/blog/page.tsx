import type { Metadata } from "next"; import Image from "next/image"; import Link from "next/link"; import { ArrowRight, Clock } from "lucide-react"; import { PageHero } from "@/components/page-hero"; import { articles } from "@/data/blog"; import { canonicalUrl } from "@/lib/commerce";
export const metadata: Metadata = { title: "Football Buying Guides", description: "Useful guides about football sizes, construction, surfaces, Sialkot manufacturing and custom orders.", alternates: { canonical: canonicalUrl("/blog/") } };
export default function Page() { return <><PageHero eyebrow="The football field guide" title="Buy with better information." description="Original, practical guidance for players, parents, coaches, schools, clubs, retailers and custom-order teams." crumbs={[{ label: "Home", href: "/" }, { label: "Journal" }]} /><section className="section"><div className="container article-grid">{articles.map((article) => <article className="article-card" key={article.slug}><Link href={`/blog/${article.slug}/`}><Image src={article.heroImage} alt={`Illustrated cover for ${article.title}`} width={720} height={440} /></Link><div><span className="eyebrow">{article.category}</span><h2><Link href={`/blog/${article.slug}/`}>{article.title}</Link></h2><p>{article.description}</p><div><span><Clock />{article.readingTime}</span><Link className="text-link" href={`/blog/${article.slug}/`}>Read guide <ArrowRight /></Link></div></div></article>)}</div></section></>; }
