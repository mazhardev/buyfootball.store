import type { Metadata } from "next"; import { CartPageClient } from "@/components/cart/cart-page-client"; import { PageHero } from "@/components/page-hero"; import { canonicalUrl } from "@/lib/commerce";
export const metadata: Metadata = { title: "Shopping Cart", description: "Review selected footballs and continue to a static order request.", alternates: { canonical: canonicalUrl("/cart/") }, robots: { index: false, follow: true } }; export default function Page() { return <><PageHero eyebrow="Saved on this device" title="Your cart." description="Review products, variants and quantities. Shipping is confirmed during order processing." crumbs={[{ label: "Home", href: "/" }, { label: "Cart" }]} /><section className="section"><div className="container"><CartPageClient /></div></section></>; }
