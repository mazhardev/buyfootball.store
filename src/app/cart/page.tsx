import { CartPageClient } from "@/components/cart/cart-page-client"; import { PageHero } from "@/components/page-hero"; import { pageMetadata } from "@/config/page-metadata";
export const metadata = pageMetadata.cart; export default function Page() { return <><PageHero eyebrow="Saved on this device" title="Your cart." description="Review products, variants and quantities. Shipping is confirmed during order processing." crumbs={[{ label: "Home", href: "/" }, { label: "Cart" }]} /><section className="section"><div className="container"><CartPageClient /></div></section></>; }
