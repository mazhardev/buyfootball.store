import { CountryPage, countryMetadata } from "@/components/country-page"; import { countries } from "@/data/countries"; const country = countries[5]; export const metadata = countryMetadata(country); export default function Page() { return <CountryPage country={country} />; }
