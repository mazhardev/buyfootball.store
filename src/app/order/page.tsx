import type { Metadata } from "next"; import { OrderForm } from "@/components/forms/order-form"; import { PageHero } from "@/components/page-hero"; import { canonicalUrl } from "@/lib/commerce";
export const metadata: Metadata = { title: "Request an Order", description: "Generate a structured WhatsApp or email order request from your saved cart.", alternates: { canonical: canonicalUrl("/order/") }, robots: { index: false, follow: true } }; export default function Page() { return <><PageHero eyebrow="No fake checkout" title="Turn your cart into an order request." description="Share accurate customer and delivery details, then send the structured request through WhatsApp or email. No payment is taken on this static site." crumbs={[{ label: "Home", href: "/" }, { label: "Cart", href: "/cart/" }, { label: "Order request" }]} /><section className="section soft"><div className="container"><OrderForm /></div></section></>; }
