import { OrderForm } from "@/components/forms/order-form"; import { PageHero } from "@/components/page-hero"; import { pageMetadata } from "@/config/page-metadata";
export const metadata = pageMetadata.order; export default function Page() { return <><PageHero eyebrow="No fake checkout" title="Turn your cart into an order request." description="Share accurate customer and delivery details, then send the structured request through WhatsApp or email. No payment is taken on this static site." crumbs={[{ label: "Home", href: "/" }, { label: "Cart", href: "/cart/" }, { label: "Order request" }]} /><section className="section soft"><div className="container"><OrderForm /></div></section></>; }
